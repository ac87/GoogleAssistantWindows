<Window x:Class="GoogleAssistantWindows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GoogleAssistantWindows"
        mc:Ignorable="d"
        Title="Google Assistant Windows" Height="350" Width="525" Loaded="MainWindow_OnLoaded" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <local:GoogleAccountControl />

            <Button x:Name="ButtonRecord" Content="Record" Grid.Column="1" HorizontalAlignment="Left" Margin="26,22,0,0" VerticalAlignment="Top" Width="134" Click="ButtonRecord_OnClick" IsEnabled="False"/>
        </Grid>

        <TextBox Grid.Row="1" x:Name="TextBoxOutput" Margin="4" HorizontalAlignment="Stretch" TextWrapping="Wrap" Text="" VerticalAlignment="Stretch" IsReadOnly="True" ScrollViewer.VerticalScrollBarVisibility="Auto" />

    </Grid>
</Window>
